spring:
  datasource:
    initialization-mode: always
    url: jdbc:h2:tcp://localhost/~/test
    username: sa
    password:

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true
